import { ComingSoon } from "../_components/coming-soon";

export default function SpreadHistoryPage() {
  return (
    <ComingSoon
      title="Spread History"
      description="A dedicated, full-history view of every tracked token's spread is on the way — for now, see the chart on Overview."
    />
  );
}
