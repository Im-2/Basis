import { ComingSoon } from "../_components/coming-soon";

export default function WalletPage() {
  return (
    <ComingSoon
      title="Wallet"
      description="Full wallet details, transaction history, and balances are on the way — for now, connect from the sidebar or top bar."
    />
  );
}
